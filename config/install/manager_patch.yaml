apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: PULL_POLICY
        envFrom:
        - secretRef:
            name: redsky-manager
            optional: true
