namespace: redsky-system

resources:
- ../default

patchesStrategicMerge:
- manager_client_patch.yaml

secretGenerator:
- name: client-config
  files:
  - client.yaml

generatorOptions:
  disableNameSuffixHash: true
