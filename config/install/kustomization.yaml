namespace: redsky-system

commonLabels:
  app.kubernetes.io/name: redskyops

resources:
- ../default

patchesStrategicMerge:
- manager_patch.yaml

secretGenerator:
- name: redsky-manager
  envs:
  - manager.env

transformers:
- metadata_labels.yaml
