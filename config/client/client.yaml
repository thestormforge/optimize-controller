# This is a sample client configuration file. Example values are wrapped in [], otherwise default values are shown.

# The address the remote API
#address: [https://example.carbonrelay.io/api]

# OAuth2 client credential configuration
#oauth2:
#  token_url: ../auth/token/
#  client_id: [xxx]
#  client_secret: [yyy]
